{
  "name": "@mashinals/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788 --local",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply mashinals-db --local",
    "db:migrate": "wrangler d1 migrations apply mashinals-db --remote"
  },
  "dependencies": {
    "@mashinals/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.1.0"
  }
}
